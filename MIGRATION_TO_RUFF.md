# Миграция на Ruff

## Что изменилось

Заменили несколько инструментов одним современным и быстрым Ruff:

### Было:
- **flake8** - линтинг
- **black** - форматирование  
- **isort** - сортировка импортов
- **bandit** - проверки безопасности

### Стало:
- **ruff** - все в одном инструменте!

## Преимущества Ruff

✅ **Скорость**: в 10-100 раз быстрее традиционных инструментов  
✅ **Простота**: один инструмент вместо четырех  
✅ **Современность**: написан на Rust, активно развивается  
✅ **Совместимость**: поддерживает все правила flake8, black, isort  
✅ **Расширяемость**: включает правила из 50+ популярных плагинов  

## Основные команды

```bash
# Проверить код (линтинг)
ruff check app/ tests/

# Исправить автоматически исправимые ошибки
ruff check app/ tests/ --fix

# Форматировать код
ruff format app/ tests/

# Проверить форматирование без изменений
ruff format --check app/ tests/

# Локальный CI
make ci-lint
# или
./scripts/ci-local.sh
```

## Конфигурация

Вся конфигурация находится в `pyproject.toml`:

```toml
[tool.ruff]
line-length = 88
target-version = "py312"

[tool.ruff.lint]
select = ["E", "W", "F", "I", "B", "C4", "UP", "S"]
ignore = ["E501", "S101", "S105"]

[tool.ruff.format]
quote-style = "double"
```

## Что исправлено автоматически

- ✅ Удалены trailing whitespaces
- ✅ Исправлено форматирование кода
- ✅ Отсортированы импорты
- ✅ Добавлены переносы строк в конце файлов
- ✅ Удалены неиспользуемые импорты
- ✅ Исправлены мелкие стилистические ошибки

## Интеграция с CI/CD

Обновлены все workflow и скрипты:
- ✅ `.github/workflows/ci.yml`
- ✅ `.github/workflows/code-quality.yml`
- ✅ `.pre-commit-config.yaml`
- ✅ `Makefile`
- ✅ `scripts/ci-local.sh`

## Оставшиеся предупреждения

Несколько предупреждений оставлены намеренно:
- `UP046` - использование старого синтаксиса Generic (для совместимости)
- `SIM117` - вложенные with statements (для читаемости)
- `B007` - неиспользуемая переменная цикла (в тестах производительности)
- `S110` - try-except-pass (в стресс-тестах)

Эти предупреждения не критичны и не влияют на работу приложения.

## Следующие шаги

1. Удалите старые конфигурационные файлы (уже сделано):
   - ~~`.flake8`~~

2. Обновите настройки редактора для использования Ruff

3. Настройте pre-commit хуки:
   ```bash
   make pre-commit-install
   ```

4. Запустите локальную проверку:
   ```bash
   ./scripts/ci-local.sh
   ```

Теперь ваш проект использует современный и быстрый инструментарий для поддержания качества кода! 🚀