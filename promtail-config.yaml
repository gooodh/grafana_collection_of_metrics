server:
  http_listen_port: 9080
  grpc_listen_port: 0

positions:
  filename: /tmp/positions.yaml

clients:
  - url: http://loki:3100/loki/api/v1/push

scrape_configs:
  - job_name: docker
    docker_sd_configs:
      - host: unix:///var/run/docker.sock
    relabel_configs:
      - source_labels: [__meta_docker_container_name]
        target_label: compose_service
        regex: "/(.*)"
      - source_labels: [__meta_docker_image]
        target_label: image
      - target_label: job
        replacement: docker
    pipeline_stages:
      - docker: {}
      - output:
          source: log


  # 🔹 2. Отдельно лог-файл приложения (если нужно)
  - job_name: fastapi
    static_configs:
      - targets:
          - localhost
        labels:
          job: fastapi
          compose_service: app
          __path__: /app/log.log
